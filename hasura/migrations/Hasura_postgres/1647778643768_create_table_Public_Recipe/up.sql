CREATE TABLE "Public"."Recipe" ("id" serial NOT NULL, "title" text NOT NULL, "given_time" Integer NOT NULL, "description" text NOT NULL, "likes" integer NOT NULL DEFAULT 0, "ratings" Integer NOT NULL DEFAULT 0.0, "created_at" date NOT NULL DEFAULT now(), "updated_at" date NOT NULL, "user_id" integer NOT NULL, PRIMARY KEY ("id") , FOREIGN KEY ("user_id") REFERENCES "Public"."User"("Id") ON UPDATE restrict ON DELETE restrict, UNIQUE ("user_id"));COMMENT ON TABLE "Public"."Recipe" IS E'Recipes';
